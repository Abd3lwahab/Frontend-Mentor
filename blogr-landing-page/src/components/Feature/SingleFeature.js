const SingleFeature = ({ title, desc }) => {
  return (
    <div className="feature-single">
      <h5 className="feature-title">{title}</h5>
      <p className="feature-desc">{desc}</p>
    </div>
  )
}

export default SingleFeature
